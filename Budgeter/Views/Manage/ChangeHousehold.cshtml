<div class="h2 text-danger">Do you really want to leave your current household?</div>
@using (Html.BeginForm("ChangeHousehold", "Manage", new { householdId = ViewBag.householdId }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{    
    <input type="submit" name="submitButton" value="Confirm" />
    <input type="submit" name="submitButton" value="Cancel" />
}